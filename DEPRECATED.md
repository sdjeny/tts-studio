# DEPRECATED — 嵌套 `app/app/` 目录

## 废弃路径

`app/app/` — 位于远程宿主机 `/work/docker/tts-studio-new/app/app/` 下的嵌套代码目录。

## 废弃原因

该目录是历史遗留的死代码（旧版代码），其功能已被项目根目录下 `app/` 中的对应文件完全替代。不再维护或使用。

## 替代方案

所有功能已迁移至项目根目录的 `app/` 下对应的文件。请直接使用 `app/` 中的模块，不要依赖或引用 `app/app/` 中的任何内容。

## 提醒

如果在容器内发现 `app/app/` 目录，请勿加载或引用其中的代码。该目录是不加载的死代码，仅作为历史参考保留，随时可能被移除。