# TTS Studio - 高级文本标记语法说明

## 概述

TTS Studio 支持通过标记语法在文本中指定不同片段的语速、音调等参数，实现一句话内多次语气变化的效果。

## 标记语法

### 基本语法

```
[标记名=值]文本内容[/标记名]
```

### 支持的标记

| 标记名 | 说明 | 示例 | 取值范围 |
|--------|------|------|----------|
| `rate` | 语速 | `[rate=-50%]慢速[/rate]` | -100% ~ +100% |
| `pitch` | 音调 | `[pitch=+20Hz]高音[/pitch]` | 无限制 |
| `volume` | 音量 | `[volume=+10%]大声[/volume]` | 无限制 |

### 示例

#### 示例1：简单语速变化

```
[rate=-40%]前半部分慢[/rate][rate=+50%]后半部分快[/rate]
```

效果：前半句慢速朗读，后半句快速朗读

#### 示例2：混合标记和纯文本

```
首先[rate=-30%]这里慢一点[/rate]然后正常[rate=+40%]这里快很多[/rate]结束
```

效果：
- "首先" - 正常语速
- "这里慢一点" - 慢速
- "然后正常" - 正常语速
- "这里快很多" - 快速
- "结束" - 正常语速

#### 示例3：嵌套标记

```
[rate=-30%]慢速[pitch=+20Hz]慢速高音[/pitch]继续慢速[/rate]正常
```

效果：
- "慢速" - rate=-30%, pitch=+0Hz
- "慢速高音" - rate=-30%, pitch=+20Hz（继承外层 rate）
- "继续慢速" - rate=-30%, pitch=+0Hz（内层 pitch 已结束）
- "正常" - rate=+0%, pitch=+0Hz

#### 示例4：复合参数

```
[rate=-30%][pitch=+10Hz]慢速高音[/pitch][/rate][rate=+40%][pitch=-10Hz]快速低音[/pitch][/rate]
```

## 工作原理

1. **解析阶段**：解析器扫描文本，提取所有标记，将文本拆分成多个片段
2. **合成阶段**：每个片段分别调用 edge-tts 合成，使用各自的参数
3. **拼接阶段**：使用 FFmpeg (pydub) 将所有片段按顺序拼接成一个完整音频

## 限制与注意事项

### edge-tts 的限制

- ✅ 支持单个 `<prosody>` 标签
- ✅ 支持两个 `<prosody>` 标签（需要足够文本）
- ❌ 不支持三个或更多 `<prosody>` 标签
- ❌ 不支持 `<break>` 停顿标签
- ❌ 不支持 `<phoneme>` 多音字标注
- ❌ 不支持 `<emphasis>` 强调标签

### 解决方案

对于超过 2 个片段的情况，系统会自动：
1. 拆分成多个独立音频文件
2. 分别合成
3. 使用 FFmpeg 拼接

### 性能考虑

- 片段越多，合成时间越长（每个片段都需要一次 API 调用）
- 建议单个句子的片段数不超过 5 个
- 临时文件会在合成完成后自动清理

## 多音字处理

edge-tts 会自动处理常见多音字，但无法精确控制。

如果需要精确控制多音字发音，有以下方案：

### 方案1：使用同音字替代（推荐）

```
# 用同音字替换多音字，合成后再替换回来
"这个重（zhòng）要的事情" → "这个众要的事情"
```

### 方案2：使用 Azure Speech Service（未来支持）

Azure Speech Service 完整支持 SSML `<phoneme>` 标签，可以精确指定拼音。

## 安装要求

### 必需

- Python 3.10+
- edge-tts 7.2.8
- pydub（用于音频拼接）

### FFmpeg（必需）

音频拼接功能需要 FFmpeg。Windows 安装方法：

#### 方法1：使用 winget（推荐）

```powershell
winget install Gyan.FFmpeg
```

#### 方法2：手动安装

1. 访问 https://www.gyan.dev/ffmpeg/builds/
2. 下载 `ffmpeg-release-essentials.zip`
3. 解压到 `C:\ffmpeg`
4. 将 `C:\ffmpeg\bin` 添加到系统 PATH

#### 验证安装

```powershell
ffmpeg -version
```

## 常见问题

### Q: 标记会被读出来吗？

A: 不会。标记只是内部指令，不会被朗读。

### Q: 可以在一行中使用多个不同标记吗？

A: 可以！系统会自动拆分并拼接。

### Q: 片段之间有停顿吗？

A: 默认没有停顿。如果需要停顿，可以使用纯文本片段分隔：

```
[rate=-30%]第一部分[/rate]。[rate=+40%]第二部分[/rate]
```

句号会产生自然停顿。

### Q: 支持中文标点吗？

A: 支持。标点符号会作为独立片段或附加到相邻片段。

### Q: 失败怎么办？

A: 系统会自动重试 3 次。如果仍然失败，会输出详细错误日志。

## 测试

运行测试脚本验证功能：

```powershell
python test_advanced_tts_e2e.py
```

测试完成后，检查 `data/audio/` 目录下的生成文件：
- `test_advanced_2seg.mp3` - 2个片段测试
- `test_advanced_3seg.mp3` - 3个片段测试
- `test_advanced_pitch.mp3` - 音调变化测试

## 更新日志

- 2024-04-13: 实现高级标记文本解析和自动拼接功能
- 2024-04-13: 添加嵌套标记支持
- 2024-04-13: 添加临时文件自动清理
